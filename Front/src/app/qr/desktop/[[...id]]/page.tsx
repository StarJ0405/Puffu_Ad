import Center from "@/components/center/Center";
import P from "@/components/P/P";

export default async function QRInfo() {
  return (
    <Center>
      <P>해당 페이지는 PC에서는 접속할 수 없습니다.</P>
    </Center>
  );
}
